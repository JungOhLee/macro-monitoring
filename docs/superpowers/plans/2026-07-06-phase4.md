# Phase 4 Implementation Plan — Calibration, Data Robustness, Analog Sharpening

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the sequencer trustworthy (evidence-backed rule fixes re-validated against all five backtest criteria), extend scored history to 1975, recalibrate regime bands from the actual score distribution, sharpen analog similarity with demeaned vectors, and de-risk the three Yahoo-blocked market series.

**Architecture:** Config + rule changes on existing machinery; every scoring/sequencer change is followed by full regeneration (scores → rebuild-episodes → seed-sequence → export → backtest) and an honest before/after criteria report. User scope decision 2026-07-06: items A, B, D (C/E deferred).

**Tech Stack:** unchanged. Suite baseline: **106 passed**.

## Global Constraints

- Repo `/Users/jolee/Library/CloudStorage/Dropbox/CodingProjects/macro-monitoring`, branch main, commit+push per task, CI green; project venv.
- **Honesty rule for every calibration change:** report all 5 backtest criteria before AND after, verbatim. A change is justified by the pre-registered forensic evidence cited in its task — never by iterating until criteria pass. If a change flips an UNRELATED criterion, stop and report before committing.
- Every task that alters pillar membership, score_start, or sequencer rules re-runs: `rm data/scores/*.csv && pipeline run && pipeline rebuild-episodes && pipeline seed-sequence && pipeline export && pipeline backtest`, commits regenerated data, and reports today's live headline (composite/stress/sequence) before vs after.
- Determinism double-run check after each regeneration.
- Commit style: prefix + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Extend scored history to 1975 (`score_start`)

**Evidence:** HE Task 1 forensics — stage 1 could not evaluate before mid-1990 because pillar score history starts 1990-01-01, starving pre-1990 backtest windows; deep raw data (CAPE 1881, Buffett 1947, margin 1997, curves 1976/82, Baa 1986) now exists.

- [ ] `config/thresholds.yaml`: `score_start: "1975-01-01"`. Expectation to verify: pre-2000 composite runs on 2-3 pillars (valuation + leverage + liquidity partials; sentiment starts ~2000 with vix qualification, macro ~2014) — reweighting handles it; confirm the earliest composite date lands ~1975 and pillar coverage per decade (report a small table).
- [ ] Full regeneration per Global Constraints. Score CSV row counts roughly +40% (~13.3k bdays full window). Backtest before/after criteria (replay unchanged at 1987 start, but stage 1 now evaluable from ~1975+126d → rec1990 stage-1 gets real history: report whether rec1990's criterion detail changes — valuation was genuinely non-extreme in 1990, so stage 1 should now evaluate FALSE with data rather than None; criterion likely still FAIL, now for purer reasons).
- [ ] History chart x-range consequence: history.json now starts 1975 — verify the chart renders (dateRange pins to data span) and markers 1980/1987/1990 (and 1973 just outside) appear on the Score History chart.
- [ ] Tests: any fixture assuming 1990 start (grep score_start / 9526 row expectations) updated meaningfully. Suite green (report count). Commit ("feat: extend scored history to 1975"), push, CI green.

### Task 2: Sequencer rule calibration

**Evidence (all pre-registered, from reviews):** (a) GFC stage-4 fired 2007-09-11 intra-month at the exact +60bp graze but month-end sampling missed it — fix the MECHANISM (intra-window evaluation with ordering), not the threshold; (b) 1990: stage 4 fired through 1989 while the 2-of-stages-1-3 gate blocked engagement — credit-led bears need an engagement path; (c) stage 5 evaluates "today at high" instead of the design's "within past month"; (d) stage-6 inputs have no staleness bound.

- [ ] `_stage_spread_widening` → intra-window with ordering: fire if within the lookback there exists a min followed (strictly later) by a value ≥ min + widen, evaluated on ALL observations in the window (not just the last): `m = w.cummin(); fired = bool(((w - m) >= cfg["widen"]).any())`. Keep `widen: 0.60`. Unit tests: fires on a mid-month spike that retraces by month-end; does NOT fire when the widening precedes the min (max-before-min case).
- [ ] `_stage_breadth_divergence`: near-high test over the past 21 observations of the index (any close within near_high_pct of the lookback max during the last month), matching design §8b "in past month". Unit test both sides.
- [ ] `_stage_price_confirmation`: staleness bounds — sahm observation ≤ 75 days old at asof, vix ≤ 10 days; older → treat that input as not-hot (document in code comment). Unit test.
- [ ] Engagement gate credit path (design amendment required in the same commit): `engaged = (≥2 of stages 1-3 active) OR (stage 3 AND stage 4 both active)`. Config: `sequencer.credit_path: true` gate so it's a visible, documented switch. Update `update_state`; unit tests: 1990-shape fixture (only stages 3+4) → engaged; stage-4-only → not engaged.
- [ ] Full regeneration + before/after criteria. **Pre-registered expectations:** gfc flips PASS (stage 4 now fires Sep-2007 → engaged via credit path or via existing stage 1+3, stage ≥4 pre-peak); rec1990 flips PASS via credit path (stages 3+4 active early 1990); dotcom unchanged (nothing new fires pre-2000 — honest); 2019 control MUST STAY PASS (verify the credit path doesn't newly engage 2019: Baa in 2019 — check whether stage 4 fired in 2019 under intra-window rule; if control flips FAIL, STOP and report, do not tune further). postcovid stays PASS. Report today's live state under the new rules (stage 4 intra-window vs today's Baa path — may legitimately differ; justify from raw values).
- [ ] Design doc §8a/§8b amendment paragraph documenting all four changes with their evidence. Suite green; commit ("feat: sequencer calibration — intra-window stage 4, credit engagement path, stage-5/6 fixes"), push, CI green.

### Task 3: Regime bands from the actual score distribution + UI single-sourcing

**Evidence:** bubble_risk ≥85 has never fired in 50 years of regenerated history; bands were spec guesses. Also app.js hardcodes band values (tracked debt).

- [ ] Compute the full-window composite's historical quantiles (post-Task-1 regeneration, 1975→). Choose round-number bands anchored to quantiles ≈ {50th, 85th, 95th} and report the mapping honestly (e.g. cool < ~55, warm < ~72, frothy < ~79, bubble_risk ≥ ~79 — EXACT values from the data, rounded to integers; do not force today's reading into a chosen band — report where today lands and where 1999/2021/1987 peaks land under the new bands).
- [ ] thresholds.yaml regime_bands update; regenerate (regime column changes; scores unchanged); alerts regime rule unaffected structurally but the change itself will likely fire a regime-change alert on the next scheduled run — note this expected one-time alert in the report.
- [ ] app.js: gauge steps, history shading, and pillar-bar color stops read from `HISTORY.regime_bands` (already exported) instead of hardcoded literals; fallback to current constants if absent. Browser-verify gauge bands match new thresholds.
- [ ] Design amendment (bands recalibrated, quantile anchors, date). Suite + browser checks; commit ("feat: regime bands recalibrated to historical score quantiles; UI reads bands from data"), push, CI green.

### Task 4: Demeaned analog similarity

**Evidence:** cosine on all-positive percentile vectors floors ~0.70 (backtest: ≥0.8 in 371/474 months outside pre-crisis windows) — weak discrimination.

- [ ] `analogs.cosine`: subtract 50.0 from every element of both vectors before the dot/norm computation (percentiles centered on neutral; similarity now spans ~[-1,1]). Zero-norm guard still applies (a perfectly-neutral vector → None). Update unit tests with recomputed expected values (recompute by hand in the test comments); keep min_shared=8 and the euclidean tiebreak (also demeaned — verify tiebreak ordering test still holds or update).
- [ ] Regenerate export + backtest. Report: new top-3 with similarities (expect visibly lower, more spread); new base rate (n_high_outside at the 0.8 threshold — likely collapses; choose and document a sensible new display threshold anchored to the new distribution, e.g. its historical 90th percentile, and update backtest base_rate threshold + analog-card copy accordingly — report the mapping, don't hide it).
- [ ] app.js analog card: no structural change (renders whatever similarity arrives); update the base-rate line copy if the threshold changed. Design amendment sentence. Suite green; commit ("feat: demeaned analog similarity for real discrimination"), push, CI green.

### Task 5: Market-data robustness (keyed source plumbing + heal)

**Evidence:** rsp/spy/btcusd Yahoo-blocked from CI and home IP since 2026-07-06; staleness alert due ~mid-July. User to supply a free Alpha Vantage or Tiingo key (pending at plan time).

- [ ] New source module `pipeline/ingest/keyed.py` implementing Alpha Vantage TIME_SERIES_DAILY_ADJUSTED (RSP, SPY) + DIGITAL_CURRENCY_DAILY (BTC) parsing, gated on env `ALPHAVANTAGE_KEY`; registry source value `alphavantage` with dispatch `keyed-then-yahoo-fallback`: if key present try keyed source, else/on-failure fall back to `fetch_yahoo`. Same sanitized-error discipline as fred.py (key in query string → failure strings must never carry it; test that). Offline unit tests (payload fixtures, key-scrub, fallback path).
- [ ] Registry: rsp/spy/btcusd source → `alphavantage` (source_id RSP/SPY/BTC). Workflow: add `ALPHAVANTAGE_KEY: ${{ secrets.ALPHAVANTAGE_KEY }}` to the Run pipeline step env (harmless if secret unset).
- [ ] Heal now regardless of key: refresh the three series through today via the established browser relay (controller assists if the implementer's headless attempts 429) so the mid-July staleness alarm resets.
- [ ] If the user's key is available by execution time: set the GH secret + .env, live-verify a keyed fetch from CI. If not: leave plumbing dormant (fallback keeps current behavior), note in README data-sources line, and the task is still complete.
- [ ] Suite green; commit ("feat: keyed market-data source with yahoo fallback; series healed"), push, CI green.

### Task 6: E2E + docs + final whole-phase review prep

- [ ] README + design §13 updated with post-calibration backtest results (honest, before/after table) and the new bands/similarity conventions.
- [ ] Live E2E: dispatch workflow, verify all pages/values; confirm expected one-time regime alert behavior (if fired, it is legitimate — note for the user, don't close).
- [ ] Hand back for final whole-phase review (range: Phase 4 commits).

## Plan Self-Review

Coverage: A → T1+T2+T3; D → T4; B → T5; honesty gates pre-registered per change; every regeneration re-seeds the sequencer (Global Constraints). Placeholders: none — T3/T4 thresholds are deliberately data-derived with mandatory honest reporting rather than pre-baked numbers (that IS the spec). Type consistency: sequencer cfg keys match existing thresholds.yaml names; `credit_path` new key defaulted true in config, read in update_state; analogs.cosine signature unchanged.
